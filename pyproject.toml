[project]
name = "ecoimpact"
version = "0.1.0"
description = "Trash map + quantified 'world fixed' impact meter."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "httpx>=0.27",
]
